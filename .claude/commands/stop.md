Stop the plan iterator loop.

## Instructions

1. Remove the loop marker:
   ```bash
   rm -f .claude/loop
   ```

2. Confirm to the user: "Plan iterator stopped. You can resume later with `/plan {ticket}` or by running `touch .claude/loop`."
